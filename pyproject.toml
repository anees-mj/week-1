[project]
name = "week-1-project"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "anees-aiit", email = "anees.mubarik@aiitsolutions.com" }
]
requires-python = ">=3.14"
dependencies = []

[project.scripts]
week-1-project = "week_1_project:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
